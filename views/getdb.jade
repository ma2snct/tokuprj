
html
  head

  body

    p jade test page

    list = ['a', 'b', 'c']
    ul
    for item, index in #{list}
      if(index % 2) == 0
        li.even = item
      else
        li.odd = item



#{out}
